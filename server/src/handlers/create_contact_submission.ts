
import { type CreateContactSubmissionInput, type ContactSubmission } from '../schema';

export declare function createContactSubmission(input: CreateContactSubmissionInput): Promise<ContactSubmission>;
